server:
  port: 9300

spring:
  application:
    name: showurs-example-kafka-consumer
  http:
    encoding:
      charset: utf-8
  servlet:
    multipart:
      max-request-size: 256MB
      max-file-size: 512MB

eureka:
  client:
    service-url:
      defaultZone: http://localhost:9000/eureka/